all:
  children:
    Windows:
      hosts:
        WindowsDesktop-1: 0c4b3c0a0000
        WindowsDesktop-2: 0c59fd860000
        WindowsDesktop-3: 0ce20ff30000
        WindowsDesktop-4: 0c4674350000
      vars:
        Adapters: '1'
        BootPriority: HDD
        ConsoleType: vnc
        OnClose: Send the shutdown signal (ACPI)
        QEMU: /usr/bin/qemu-system-x86_64(v4.2.1)
        RAM: '4096'
        ReplicateNetworkConnectionStatesInQEMU: 'True'
        Type: Intel Gigabit Ethernet (e1000)
        vCPUs: '2'
